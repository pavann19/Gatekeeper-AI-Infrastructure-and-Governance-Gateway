# streamlit was the dependency of a Streamlit dashboard prototype
# (ui/web_app.py) removed in Phase 8 hardening -- no code in core/, api/, or
# tests/ imports it. Dropped rather than version-bumped: pip-audit flags two
# CVEs against it (incl. an unauthenticated SSRF on Windows hosts,
# PYSEC-2026-2285), and removing a dead dependency closes both plus its
# transitive tree, instead of just moving the floor forward.
requests>=2.31.0
sentence-transformers>=2.3.1
numpy>=1.26.0,<2.0.0
spacy>=3.7.0
diagrams>=0.23.0
python-dotenv>=1.0.0
python-json-logger>=2.0.7
pydantic-settings>=2.0.0
PyYAML>=6.0
fastapi>=0.109.0
uvicorn>=0.27.0
httpx>=0.26.0
# >=9.0.3 closes PYSEC-2026-1845 (predictable /tmp/pytest-of-{user} dir name).
pytest>=9.0.3
pytest-cov>=4.1.0
ruff>=0.6.0
faiss-cpu>=1.7.4
psutil>=5.9.0
redis>=5.0.0
prometheus-client>=0.20.0
nemoguardrails>=0.23.0
einops>=0.8.0
