# CI-only requirements (lightweight where possible; no heavy ML MODEL
# DOWNLOADS needed for unit tests — sentence-transformers and real HF weights
# are still excluded/mocked). torch and faiss-cpu ARE included: several tests
# exercise the real FAISSCache index (tests/test_cache.py) and real tensor
# shape/label-resolution logic (tests/test_detectors.py) against synthetic
# data, not mocks of those libraries themselves — the cache exact-match bug
# this project fixed was exactly the kind of thing a faiss-free mock could
# have hidden. Both are plain pip packages here, no model weights fetched.

fastapi>=0.109.0
uvicorn>=0.27.0
httpx>=0.26.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
python-json-logger>=2.0.7
requests>=2.31.0
numpy>=1.26.0,<2.0.0
spacy>=3.7.0
pytest>=8.0.0
pytest-asyncio>=0.23.0
torch>=2.0.0
faiss-cpu>=1.7.4
# psutil is NOT a transitive dependency of torch/transformers/faiss, and was
# missing from BOTH requirement files until this line — meaning
# LlamaGuardDetector's memory precheck (core/detectors.py) silently no-ops
# whenever psutil happens to be absent, in CI or in a real deployment, and the
# detector proceeds to attempt loading a multi-GB model regardless of whether
# it fits in memory. Declaring it explicitly is a correctness fix, not just a
# CI convenience.
psutil>=5.9.0
