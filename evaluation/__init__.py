"""Evaluation library — metrics, slicing, and suite loading.

Kept separate from `core/` because none of it runs in production. `core/` is the
serving path; `evaluation/` is the measurement apparatus.
"""
