# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/

# Virtual environments
.env
.env*
.venv
env/
venv/
ENV/

# Local State, Caches, & Runtime Artifacts
audit.jsonl
semantic_cache.json
semantic_cache_exact.json
evaluation_results.txt
.pytest_cache/
.coverage
logging/
*.jsonl
!tests/fixtures/decision_replay_corpus.jsonl

# Agent/debug scaffolding — must never be committed
debug-*.log

# Evaluation intermediates. The calibration REPORT and curves are evidence and
# are committed; the raw signal cache is a regenerable multi-MB intermediate.
_evidence/calibration_signals.json
_evidence/evaluation_results.csv
benchmark_rows.csv

# Cached sentence embeddings (scripts/build_multilingual_feature.py) --
# regenerable in minutes from data/eval_suite.jsonl, ~20MB, not evidence.
_evidence/embeddings/

# Notes, Clutter & Archive (Crucial: never commit the archive folder to GitHub)
Notes.txt
Gatekeeper_Technical_Report.md
Gatekeeper_Technical_Report.pdf
archive/

# IDE
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Secrets
*.key
*.pem

# Binary & reports
*.pdf

# Local HuggingFace model cache (redirected off the C: drive)
.hf_cache/

# Evaluation run logs (tqdm noise; the JSON reports are the evidence)
_evidence/*.log

# API key store (hashed keys, but still a credential inventory)
api_keys.json

# Local tenant fixtures (e.g. from live pentest setup) -- not real deployment config
tenants.json
